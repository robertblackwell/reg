extern crate wpool;

use reg::server::*;

fn main() {

    let server = Server{
        m_nbr_workers   : 5,
        m_host          : "localhost".to_string(),
        m_port          :9001
    };
    server.listen();
}
